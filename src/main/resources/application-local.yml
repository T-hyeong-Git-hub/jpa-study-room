spring:
  datasource:
    url: jdbc:mysql://localhost:3307/studyroom?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: study
    password: study1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
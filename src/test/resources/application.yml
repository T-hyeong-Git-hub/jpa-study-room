spring:
  application:
    name: jpaStudyRoom
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: test

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

